# Should set logging level to ERROR on production, and INFO on dev / test
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dbpaperdb
    username: vlatko
    password: vlatko
  jpa:
    hibernate:
      dll-auto: validate
    open-in-view: false
  flyway:
    locations:
      - classpath:db/migration
logging:
  file:
    path: logs
database-management:
  import-all: false
  import-notability-index: false
  process-citizenship-names-readability: false
  process-citizenship-names-search: false
  process-person-names-search: false
search:
  results-per-page: 1000